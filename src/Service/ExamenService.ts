import { ExamenPublique } from '../model/Examen';

export class ExamenService {
    async getAllExams(): Promise<ExamenPublique[]> {
        const examens: ExamenPublique[] = [];
    return examens;
  }
}